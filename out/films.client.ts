// generated by beeline from 3 recorded runs.
// GET https://www.scrapethissite.com/pages/ajax-javascript/
// 2026-09-19T23:47:15.573Z
//
// do not edit: re-run `npm run synth -- films` instead

export type FilmsResponse = {
  title: string;
  year: number;
  awards: number;
  nominations: number;
  best_picture?: boolean;
}[];

export interface FilmsParams {
  year: string;
}

export class FilmsClient {
  private session: Record<string, string> = {};
  private cookies: Record<string, string> = {};

  async call(params: FilmsParams): Promise<FilmsResponse> {

    const url = new URL("https://www.scrapethissite.com/pages/ajax-javascript/");
    url.searchParams.set("ajax", "true");
    url.searchParams.set("year", params.year);
    const res = await fetch(url, {
      method: "GET",
      headers: {
        "accept": "*/*",
        "accept-language": "en-US,en;q=0.9",
        "referer": "https://www.scrapethissite.com/pages/ajax-javascript/",
        "user-agent": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/153.0.0.0 Safari/537.36",
        "x-requested-with": "XMLHttpRequest",
      },
    });

    if (!res.ok) {
      throw new Error(`films: ${res.status} ${res.statusText}`);
    }

    return (await res.json()) as FilmsResponse;
  }

  private cookieHeader(): string {
    return Object.entries(this.cookies)
      .map(([k, v]) => `${k}=${v}`)
      .join("; ");
  }
}

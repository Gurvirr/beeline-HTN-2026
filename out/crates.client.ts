// generated by beeline from 3 recorded runs.
// GET https://crates.io/api/v1/crates
// 2026-09-20T13:19:58.472Z
//
// do not edit: re-run `npm run synth -- crates` instead

export type CratesResponse = {
  crates: {
    id: string;
    name: string;
    updated_at: string;
    versions: null;
    keywords: null;
    categories: null;
    badges: unknown[];
    created_at: string;
    downloads: number;
    recent_downloads: number;
    default_version: string;
    num_versions: number;
    yanked: boolean;
    max_version: string;
    newest_version: string;
    max_stable_version: string;
    description: string;
    homepage: string;
    documentation: string;
    repository: string;
    links: {
      version_downloads: string;
      versions: string;
      owners: string;
      owner_team: string;
      owner_user: string;
      reverse_dependencies: string;
    };
    exact_match: boolean;
    trustpub_only: boolean;
  }[];
  meta: {
    total: number;
    next_page: string;
    prev_page: null;
  };
};

export interface CratesParams {
  q: string;
}

export class CratesClient {
  private session: Record<string, string> = {};
  private cookies: Record<string, string> = {};

  async call(params: CratesParams): Promise<CratesResponse> {

    const url = new URL("https://crates.io/api/v1/crates");
    url.searchParams.set("page", "1");
    url.searchParams.set("per_page", "10");
    url.searchParams.set("q", params.q);
    url.searchParams.set("sort", "relevance");
    const res = await fetch(url, {
      method: "GET",
      headers: {
        "accept": "*/*",
        "accept-language": "en-US,en;q=0.9",
        "referer": "https://crates.io/search?q=serde",
        "user-agent": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/153.0.0.0 Safari/537.36",
      },
    });

    if (!res.ok) {
      throw new Error(`crates: ${res.status} ${res.statusText}`);
    }

    return (await res.json()) as CratesResponse;
  }

  private cookieHeader(): string {
    return Object.entries(this.cookies)
      .map(([k, v]) => `${k}=${v}`)
      .join("; ");
  }
}

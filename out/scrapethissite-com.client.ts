// generated by beeline from 3 recorded runs.
// GET https://www.scrapethissite.com/pages/ajax-javascript/
// 2026-09-20T01:12:43.256Z
//
// do not edit: re-run `npm run synth -- scrapethissite-com` instead

export type ScrapethissiteComResponse = {
  title: string;
  year: number;
  awards: number;
  nominations: number;
  best_picture?: boolean;
}[];

export interface ScrapethissiteComParams {
  q: string;
}

export class ScrapethissiteComClient {
  private session: Record<string, string> = {};
  private cookies: Record<string, string> = {};

  async call(params: ScrapethissiteComParams): Promise<ScrapethissiteComResponse> {

    const url = new URL("https://www.scrapethissite.com/pages/ajax-javascript/");
    url.searchParams.set("ajax", "true");
    url.searchParams.set("year", params.q);
    const res = await fetch(url, {
      method: "GET",
      headers: {
        "accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7",
        "accept-language": "en-US,en;q=0.9",
        "user-agent": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/153.0.0.0 Safari/537.36",
      },
    });

    if (!res.ok) {
      throw new Error(`scrapethissite-com: ${res.status} ${res.statusText}`);
    }

    return (await res.json()) as ScrapethissiteComResponse;
  }

  private cookieHeader(): string {
    return Object.entries(this.cookies)
      .map(([k, v]) => `${k}=${v}`)
      .join("; ");
  }
}
